[package]
name = "apios-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ApiosCleaner engine: portable Rust core for app file scanning, matching, orphan detection, trash semantics, and Mach-O fat-binary parsing"
repository.workspace = true
readme.workspace = true
homepage.workspace = true
keywords = ["cleaner", "macos", "app", "scanning", "mach-o"]
categories = ["command-line-utilities"]

[dependencies]
walkdir = "2"
rayon = "1"
plist = "1"
regex = "1"
chrono = "0.4"
libc = "0.2"

[dev-dependencies]
tempfile = "3"
