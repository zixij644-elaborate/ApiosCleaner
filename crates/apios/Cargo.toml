[package]
name = "apios"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ApiosCleaner 命令行工具（命令参数式）"

[dependencies]
apios-core = { path = "../apios-core" }
clap = { version = "4", features = ["derive"] }
